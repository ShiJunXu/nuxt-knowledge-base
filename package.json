{
  "name": "nuxt-knowledge-base",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "build:cloudflare": "nuxt build --preset=cloudflare_pages",
    "dev": "nuxt dev",
    "dev:cloudflare": "wrangler pages dev dist --d1 DB=knowledge_base",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "@nuxtjs/color-mode": "latest",
    "nuxt": "latest",
    "vue": "latest",
    "vue-router": "latest"
  },
  "devDependencies": {
    "typescript": "latest",
    "wrangler": "latest"
  }
}
